{
  "name": "silvas-accounting",
  "version": "1.0.0",
  "description": "Accounting management system",
  "engines": {
    "node": "18.x",
    "npm": "9.x"
  },
  "scripts": {
    "start": "node src/server.js",
    "heroku-postbuild": "cd _frontend && npm install && npm run build"
  }
}
