const { v4: uuidv4 } = require('uuid');

module.exports = {
  up: async (queryInterface) => {
    const reports = [
      {
        id: uuidv4(),
        reportNumber: `RPT-EXTRA2-${Math.floor(Math.random() * 9000) + 1000}`,
        name: 'Extra Profit Report Two',
        description: 'Second additional profit report generated by seeder',
        type: 'profit_and_loss',
        reportData: { profit: 32000, period: 'Q3' },
        generatedBy: 'seeder',
        generatedDate: new Date(),
        startDate: new Date(new Date().getFullYear(), 6, 1),
        endDate: new Date(new Date().getFullYear(), 8, 30),
        status: 'completed',
        format: 'json',
        fileUrl: null,
        isPublic: false,
        createdAt: new Date(),
        updatedAt: new Date()
      }
    ];

    await queryInterface.bulkInsert('reports', reports);
  },
  down: async (queryInterface) => {
    await queryInterface.bulkDelete('reports', { name: 'Extra Profit Report Two' }, {});
  }
};
