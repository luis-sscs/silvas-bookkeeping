const { v4: uuidv4 } = require('uuid');

module.exports = {
  up: async (queryInterface, Sequelize) => {
    const reports = [
      {
        id: uuidv4(),
        reportNumber: `RPT-EXTRA-${Math.floor(Math.random() * 9000) + 1000}`,
        name: 'Extra Revenue Report',
        description: 'Additional revenue report generated by seeder',
        type: 'income',
        reportData: JSON.stringify({ totalRevenue: 50000, period: 'Q2' }),
        generatedBy: 'seeder',
        generatedDate: new Date(),
        startDate: new Date(new Date().getFullYear(), 3, 1),
        endDate: new Date(new Date().getFullYear(), 5, 30),
        status: 'completed',
        format: 'json',
        fileUrl: null,
        isPublic: false,
        createdAt: new Date(),
        updatedAt: new Date()
      }
    ];

    await queryInterface.bulkInsert('reports', reports);
  },

  down: async (queryInterface, Sequelize) => {
    await queryInterface.bulkDelete('reports', { name: 'Extra Revenue Report' }, {});
  }
};
